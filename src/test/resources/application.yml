# H2
# datasource
spring:

  datasource:
    url: jdbc:h2:mem:test-db;MODE=MYSQL;
    username: sa
    password:

  jpa:
    hibernate:
      ddl-auto: none # validate # update # create-drop # create
